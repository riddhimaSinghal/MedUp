
  # Booking Website (Copy)

  This is a code bundle for Booking Website (Copy). The original project is available at https://www.figma.com/design/8AlW5N6NflRFAdiEqdRPMa/Booking-Website--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  